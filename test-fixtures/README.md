# Test fixtures

Tiny **synthetic** artifacts only (KB scale). No real datasets, weights, or cluster logs.

Phase 1.2 will add concrete tensor/JSON fixtures and a regeneration script.
Until then, unit tests construct data in-memory.
